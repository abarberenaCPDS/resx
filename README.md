# resx

### Git Alias

```sh
alias.fb=!git fetch && git branch -vv
alias.lol=log --pretty=format:'%C(auto)%h%Creset%C(auto)%d %Creset%ai %C(auto,green)%aN %C(auto,blue)%m%C(reset) %s' --graph --color --date=short --color=always
```